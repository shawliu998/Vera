/** Keep this aligned with the backend and isolated desktop credential worker. */
export const VERA_MODEL_CREDENTIAL_MAX_UTF8_BYTES = 1024;

/** Legal-source secrets use encrypted local storage, not macOS Keychain. */
export const VERA_LEGAL_SOURCE_CREDENTIAL_MAX_CHARACTERS = 32_768;
