{
  "name": "vera-connected-desktop",
  "version": "1.1.0",
  "private": true,
  "description": "Vera connected legal workspace for macOS.",
  "author": "Vera contributors",
  "license": "AGPL-3.0-only",
  "main": "connectedMain.js",
  "scripts": {
    "start": "electron connectedMain.js",
    "test:connected-security": "node scripts/connectedDesktopAudit.js",
    "test:connected-smoke": "node scripts/connectedDesktopSmoke.js",
    "test:connected": "npm run test:connected-security && npm run test:connected-smoke",
    "signing:preflight": "node scripts/releaseSigningPreflight.js",
    "signing:readiness": "node scripts/macReleaseReadiness.js",
    "signing:verify": "node scripts/verifyMacRelease.js",
    "test:signing-pipeline": "node scripts/releaseSigningAudit.js",
    "pack:mac": "npm run signing:preflight && electron-builder --mac --dir",
    "dist:mac": "npm run signing:preflight && electron-builder --mac"
  },
  "devDependencies": {
    "@electron/notarize": "2.5.0",
    "electron": "^39.2.7",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "ai.vera.desktop",
    "productName": "Vera",
    "asar": true,
    "afterSign": "scripts/afterSign.js",
    "afterAllArtifactBuild": "scripts/afterAllArtifactBuild.js",
    "files": [
      "connectedMain.js",
      "connectedPreload.js",
      "connectedConfig.js",
      "package.json",
      "build/icon.icns",
      "build/icon.png"
    ],
    "extraResources": [],
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "build/icon.icns",
      "target": [
        "dmg",
        "zip"
      ],
      "hardenedRuntime": true,
      "notarize": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.inherit.plist",
      "gatekeeperAssess": false,
      "artifactName": "Vera-Connected-${version}-${arch}.${ext}"
    },
    "dmg": {
      "artifactName": "Vera-Connected-${version}-${arch}.${ext}",
      "sign": true,
      "writeUpdateInfo": false
    }
  }
}
